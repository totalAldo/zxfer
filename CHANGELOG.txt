CHANGELOG
=========

2026.04.11
-Reopen the rolling send/receive completion-queue writer after job-limit waits, and if that reopen fails drain the remaining rolling jobs before falling back to legacy background waits, so recursive `-j` replications no longer silently skip some sends until a later zxfer pass picks them up.
-Keep per-run source and destination snapshot-record caches on disk past the initial recursive diff and make per-dataset common-snapshot/delete planning prefer those file-backed caches over the large in-memory list strings, so later datasets no longer get their newest destination snapshots missed on shells that truncate oversized command-substitution payloads during recursive `-R ... -d ... -j ...` runs.
-Run delete/common-snapshot snapshot-record lookups in the current shell, register cache-object stage directories for trap-time cleanup, switch ssh control-socket and remote-capability cache roots to run-unique temp-prefix directories, and keep pruning empty legacy shared roots on exit so interrupted or completed runs stop leaving `zxfer-snapshot-index.*`, `zxfer-s.*.d`, or `zxfer-remote-capabilities.*.d` directories behind in the temp root while concurrent zxfer invocations no longer collide on the same cache-root names.
-Register secure same-directory error-log and backup-metadata stage directories with the runtime cleanup inventory, and only mark backup rollback files for trap cleanup after the new metadata is already live, so interrupted runs reap stale `/tmp`-style helper artifacts without deleting rollback state that may still be needed for recovery.
-Reap legacy non-group-writable ssh control-socket lock directories left behind by older branch runs, surface the specific lock failure through setup/cleanup diagnostics, and keep remote capability probes on one shell line so csh/tcsh login shells no longer choke on multiline helper payloads.
-Drop acceptance of the retired `ZXFER_REMOTE_CAPS_V1` remote capability payload, keep the live probe and mock/integration fixtures on `ZXFER_REMOTE_CAPS_V2` only, and treat stale V1 cache payloads as cache misses so remote-helper bootstrap no longer carries dead protocol branches.
-Stop writing the legacy `#initial_source` and `#destination` backup-metadata aliases, keep only the current-format `#format_version`, `#source_root`, and `#destination_root` header markers, and refresh the fixtures, tests, and docs to match.
-Disable the GitHub Actions `posh` portable-shell lane for now and comment the workflow accordingly because the hosted run time exceeds 30 minutes.
-Record pid metadata in per-entry ssh control-socket locks, reap dead-lock holders immediately plus pidless leftover lock directories after the existing wait window, and make local lock release remove the pid file and report cleanup failures, so stale `.lock` directories no longer wedge later `-O`/`-T` runs behind a generic `Error creating ssh control socket ...` failure.
-Make `-V` print prefixed live remote ssh commands, remote probe commands, and ssh control-socket check/open commands before execution, so remote-startup stalls now show the exact in-flight bootstrap or probe command on stderr.
-Reduce startup/no-op latency on remote `-O ... -j ... -z/-Z` runs by keying the per-host capability handshake/cache by requested tool scope, preloading only the minimum startup `zfs` capability during connection setup, deferring origin-host `parallel`, `cat`, and compression/decompression helper probes until later on-demand resolution, serving generic remote helper lookups from scoped cached capability data before opening a second ssh probe, adding bounded fast-retry phases before the older whole-second ssh-control-socket and capability-cache waits, removing temp-file staging from the current-shell GNU `parallel` availability check, and extending `-V` profiling with ssh-lock wait, capability-cache wait, capability-bootstrap source, and direct remote helper probe counters.
-Simplify remote `-O ... -j ...` GNU parallel handling by trusting any resolved origin-host `parallel` helper, keeping only the missing-helper serial fallback, and failing closed later through the staged source snapshot-list stderr path if the rendered remote listing cannot execute.
-Treat blank destination property-support probe failures as property-level rejections only after a generic dataset property listing succeeds, so OmniOS `-U` replications no longer abort when unsupported pool-root probes exit nonzero without stderr while real destination query failures still stop the run with a non-empty diagnostic.
-Accept usable `size` records from exact `zfs send -nPv` progress-estimate probes even when the dry-run helper exits nonzero, so Linux integration runs no longer abort `-D ... %%size%%` transfers after the estimate probe prints a valid size payload alongside a nonzero status.
-Keep adaptive remote `-j` GNU parallel fallback diagnostics staged under a collision-proof current-shell temp-file variable, so nested remote-probe helpers on dash and other POSIX shells no longer clobber the staged path and drop the original non-GNU fallback reason from verbose output.
-Restore remote source snapshot-list metadata compression on adaptive `-O ... -j ... -z/-Z` discovery runs so the source metadata path again honors the selected ssh compression pipeline, and refresh the docs and shunit coverage to match.
-Add `--test-layer integration|shunit2` to `tests/run_vm_matrix.sh`, keep guest-backed integration as the default layer, route both the local `qemu` and `ci-managed` backends through one selected guest test command, skip integration-only package installs for Ubuntu and FreeBSD shunit guest runs, and install plus wrap `bash --posix` automatically for OmniOS shunit guest runs so the existing FreeBSD/OmniOS/Ubuntu VM guest paths can exercise `tests/run_shunit_tests.sh` without changing the default integration workflow.
-Rewrite shunit-runner/test signal probes to POSIX `kill -s` syntax, remove one dead runner helper, and avoid word-splitting in the replay queue head pop so the pinned ShellCheck and checkbashisms CI targets stay clean without changing worker-shutdown behavior.
-Normalize runtime-artifact staged-write redirection-open failures to exit status `1` across `/bin/sh` implementations, so Ubuntu/dash unit runs no longer see a shell-specific status `2` when a silent temp-file write fails before the helper can publish operator-facing errors.
-Run `tests/run_shunit_tests.sh` through a bounded parallel worker queue by default, add `--jobs` for explicit control, clamp explicit overprovisioned job counts to the runnable suite count with a notice, keep `--jobs 1` on the historical live foreground output path, free parallel worker slots as soon as any suite finishes while still replaying output in input order, hard-stop TERM-resistant suites during runner shutdown in both serial and parallel modes, close the worker child-launch signal race before suite PIDs are recorded, and isolate the remote-host shunit fallback temp roots under suite-private short directories so unit runs can parallelize without racing shared ssh-control-socket or remote-capability cache paths.

2026.04.10
-Preserve exact nonzero statuses through the public property-cache wrappers and fail recursive property-tree prefetch closed when grouped cache application cannot be read back or stored cleanly, so local cache staging failures no longer degrade into generic exit status `1` or a falsely complete prefetch pass.
-Harden internal capability, property, and snapshot-record caches around one shared whole-object file/directory format with staged publish plus validation, so truncated, headerless, unreadable, or partially published cache payloads now invalidate the entire object and fall back to a live probe or index rebuild instead of being accepted as empty success.
-Preserve remote GNU `parallel` functional-probe exit statuses across `if` conditionals and emit adaptive snapshot-discovery fallback notices on stderr in normal verbose mode, so FreeBSD origin-host non-GNU and missing-helper checks now reliably drop to the serial listing path and still explain the fallback during `-v` runs.
-Keep adaptive remote source-discovery GNU `parallel` resolution in the current shell instead of inside a command substitution, so origin-host parallel validation now preserves the resolved helper path for the later rendered remote pipeline and no longer degrades successful GNU `parallel` checks into empty-command `Permission denied` failures.
-Capture exact `zfs send -nPv` progress-estimate probe output through current-shell temp staging before testing its exit status, so Linux `/bin/sh` runs preserve the original probe and parse failure text instead of sometimes emitting blank exact-estimate diagnostics.
-Replace regex-based source-to-destination dataset prefix stripping with one shared literal-path mapping helper, so legal dataset names containing regex-significant characters such as `.` no longer compute the wrong receive destination, forwarded backup destination, or unsupported-property probe target.
-Fail closed when recursive snapshot planning setup or lazy identity validation fails inside `zxfer_get_zfs_list()`, and preserve the original nonzero status from tempfile allocation and recursive source-list planning instead of continuing with stale or empty recursive dataset lists.
-Fail closed when staged property-record serializer output cannot be reloaded, and preserve that original readback status through the public normalized-property getter, so live normalized-property and required-property probes no longer degrade serializer staging failures into empty payloads or a generic exit status `1`.
-Fail closed when shared snapshot-discovery staged-file reloads cannot be read back, so staged source commands, destination inventories, and related discovery payloads no longer degrade readable-but-unopenable capture paths into empty or partial discovery state.
-Treat rolling `-j` send/receive completion reporting as a checked operation by separating the parent read/write FIFO fds, persisting structured background-job status records, and falling back cleanly when queue notifications are lost, so background status-file and completion-publication failures no longer hang the rolling pool or degrade into generic child exit handling.
-Treat partial property-cache file reloads as cache misses by failing closed on staged cache-file read errors, so normalized-property and required-property lookups no longer accept truncated cached payloads instead of retrying live probes.
-Fail closed when staged remote probe stdout/stderr cannot be reloaded from local capture files, and surface those reload failures distinctly in remote backup directory, backup read, and backup write paths instead of misreporting them as ssh transport problems.
-Fail closed when unsupported-property discovery cannot reload its staged source property list, so direct destination support probes no longer degrade staged readback failures into partial scans that silently miss unsupported properties.
-Preserve sorted staged-list readback failures in replication iteration-list and post-seed property-source assembly, so zxfer now aborts with the original readback status instead of degrading those failures into partial dataset loops or silently skipped deferred property work.
-Fail closed when `ssh -O check` / `ssh -O exit` stderr cannot be reloaded from staged local capture, so shared control-socket setup and teardown no longer degrade staged stderr read failures into blank live/stale/error diagnostics.
-Read staged recursive destination dataset inventories through checked current-shell scratch and fail closed when the readback fails or is unexpectedly empty, so destination discovery no longer seeds an empty recursive tree from corrupt staging.
-Treat unreadable or empty property-cache hits as cache misses and fall back to live property probes, so normalized-property and required-property lookups no longer degrade cache read failures into empty cached results.
-Accept GNU `parallel` helpers whose version banners are wrapper-altered or otherwise nonstandard by falling back to a GNU-specific functional probe before rejecting them, so origin hosts no longer misclassify working GNU `parallel` installs as non-GNU when the banner text is inconclusive.
-Read staged source snapshot-list commands through checked current-shell scratch and fail closed when the temp-file readback fails or returns empty content, so source snapshot discovery no longer degrades staged-command read failures into blank commands or opaque later errors.
-Build recursive snapshot-delta dataset lists and identity-validation candidate sets through checked current-shell staging, and fail closed on `comm`/`awk`/`sort`/exclude-filter errors, so recursive planning no longer degrades those failures into empty or partial dataset lists that silently skip source or destination datasets.
-Stop treating origin-side GNU `parallel` validation failures as fatal to `-j`, and fall back to the serial source snapshot-listing path instead, so runs still keep concurrent send/receive jobs when the fast discovery helper is missing, non-GNU, or misvalidated.
-Build the replication iteration list through checked current-shell scratch and fail closed when the merge, temp staging, or dedupe path fails, so recursive property/delete planning no longer degrades list-construction failures into partial or empty replication loops that silently skip datasets.
-Allocate the post-seed property-reconcile queue through the current-shell temp-file contract, check every seeded-source append, and fail closed when the final dedupe/readback step fails, so seeded destinations no longer skip their deferred property pass or backup finalization after queue-staging errors.
-Make remote GNU `parallel` validation fall back from non-GNU `--will-cite --version` banners to a plain `--version` probe before rejecting the helper, so origin hosts whose `--will-cite` path emits citation-only text no longer fail `-j` source discovery with a false `is not GNU parallel` error.
-Allocate recursive property-tree prefetch staging files through the current-shell temp-file contract and fail closed when the grouped machine/human merge step fails, so recursive normalized-property prefetch no longer degrades temp or merge failures into cache-miss fallback behavior.
-Stop consuming normalized-property and required-property serializers through unchecked command substitutions, so exact property reads now fail closed on serializer errors instead of caching empty property payloads or degrading into generic parse failures.
-Restore the FreeBSD and OmniOS GitHub Actions integration lanes to the direct guest-local `vmactions` flow used on `main`, so those jobs no longer invoke `tests/run_vm_matrix.sh --backend ci-managed` inside FreeBSD or OmniOS guests and instead install native prerequisites, run `./tests/run_integration_zxfer.sh --yes --keep-going` directly, and upload the preserved guest workdirs from the original artifact paths.
-Allocate the remaining live snapshot-discovery temp files through the current-shell `g_zxfer_temp_file_result` contract, so source command staging, reverse helpers, and `zxfer_get_zfs_list()` now fail closed with the original tempfile diagnostic instead of degrading into generic discovery errors.
-Refresh the per-run local guest `known_hosts` file before each SSH-backed step and require OmniOS local QEMU guests to hold a stable SSH host key across multiple readiness probes, so the r151056 cloud image on macOS/arm64 and other hosts no longer trips `REMOTE HOST IDENTIFICATION HAS CHANGED` or transient first-boot SSH resets between readiness, preparation, and harness execution.
-Fail recursive snapshot delta planning closed on `comm`, `awk`, identity-file, and identity-diff failures, and stop leaking current-shell temp-file paths while doing so, so recursive source/destination trees no longer look synchronized when diff or identity validation actually failed.
-Restore the Ubuntu GitHub Actions integration lane to the direct-host `ubuntu-24.04` implementation used on `main`, so CI no longer depends on the newer self-hosted QEMU guest path for Linux ZFS integration and instead installs `zfsutils-linux`, loads `zfs`, and runs `./tests/run_integration_zxfer.sh --yes --keep-going` under `sudo` with preserved failure artifacts.
-Fail snapshot deletion closed when identity generation, `comm`, or identity-to-path mapping fails, and propagate that nonzero status out of `zxfer_delete_snaps()` instead of degrading the failure into an empty delete set with `No snapshots to delete.`
-Allocate rolling `-j` background-job status files through the current-shell `g_zxfer_temp_file_result` contract before forking, so send/receive status tracking now preserves the original tempfile-allocation failure instead of spawning a background job with an empty status path and degrading later into generic PID/status-matching errors.
-Allocate property-reconciliation scratch files through the current-shell `g_zxfer_temp_file_result` contract instead of `$(zxfer_get_temp_file)`, so property filtering, source-property collection, unsupported-property scans, parent reconciliation, and transfer-time diff staging now fail closed with the original tempfile diagnostic instead of degrading into misleading property errors or empty buffers.
-Stage remote probe stdout/stderr under one private temp directory allocated in the current shell, so probe tempfile-allocation failures now abort with the original `Error creating temporary file.` diagnostic instead of degrading into generic remote capability, uname, or helper-query probe errors.
-Preserve stderr from ssh control-socket `-O check` / `-O exit` actions, classify explicitly stale masters separately from transport/policy failures, restore the last shared lease when shutdown fails, and stop setup from silently replacing an existing socket after a failed close, so control-socket teardown no longer discards local cache state or hides managed-ssh/bootstrap problems behind a fake clean shutdown.
-Render grandfather-protection snapshot dates from the already-validated numeric creation epoch instead of issuing a second unchecked `zfs get creation` query, so old-snapshot `-g` errors no longer hide remote transport/query failures behind a blank or misleading `Snapshot date:` field.
-Pass `--will-cite` on local and remote GNU parallel version validation probes, with a plain `--version` fallback for non-GNU implementations, so `-j` validation no longer rejects real GNU Parallel installs that emit citation-policy output until the notice is acknowledged.
-Preserve multiline remote `sh -c` payloads as a single `-c` argument, so remote helper probes such as origin-host `parallel --version` checks no longer run only the first line of their generated probe script and falsely reject valid GNU Parallel installs with citation preambles.
-Allocate the three snapshot-delete scratch files lazily when delete planning actually runs, so non-delete replication flows no longer seed unused startup tempfiles that can leak on early SIGINT exits.
-Add `--only-test name[,name...]` to `tests/run_integration_zxfer.sh` and pass it through `tests/run_vm_matrix.sh`, so humans and Codex can cherry-pick named integration cases, fail closed on unknown test names, and keep guest-backed iteration loops tight without rerunning the full selected profile every time.
-Normalize multiline `parallel --version` output before GNU-signature matching, so remote and local `-j` validation now accepts citation-preamble banners reliably on FreeBSD and other platforms that prepend extra text before the GNU Parallel version line.
-Make `tests/run_vm_matrix.sh --failed-tests-only` automatically imply live guest output streaming, so AI-assisted and other failure-only runs still surface the guest harness's compact `[N/TOTAL] PASS test_name` and `[N/TOTAL] SKIP test_name` liveness lines without requiring a second flag.
-Preserve the already-captured pre-seed `-k` backup row when deferring seeded destination metadata for post-seed reconciliation, so chained backups no longer overwrite forwarded upstream provenance with the intermediate dataset's live properties before a later `-e` restore.
-Preserve stderr from destination snapshot `creation` probes and fail delete/grandfather planning closed on transport or query failures, so remote ssh/auth/bootstrap errors no longer degrade into empty creation times, generic grandfather messages, or rollback-eligible false positives.
-Preflight ssh transport validation before allocating local remote-probe capture temp files, so invalid managed-ssh policy or other transport-helper setup failures no longer leak secure probe temp files under the runtime temp root.
-Preserve ssh/bootstrap diagnostics for remote backup-directory preparation and remote `-k` backup-metadata writes instead of collapsing host-key, authentication, wrapper/bootstrap, and managed-ssh policy failures into generic backup-directory or mounted-filesystem write errors.
-Preserve transport/bootstrap diagnostics for opportunistic remote capability preloads by re-emitting live capability-fetch failures through `zxfer_ensure_remote_host_capabilities()` and surfacing them during connection setup in verbose mode instead of dropping every nonzero preload result silently.
-Make restore-mode backup metadata reads consume local and remote backup-file payloads through current-shell scratch state instead of `$(zxfer_read_*_backup_file ...)`, so remote dependency/bootstrap failures and local secure-file guard failures no longer degrade into the generic unreadable-backup error after the specific probe diagnostic has already been emitted.
-Add `--failed-tests-only` to both `tests/run_integration_zxfer.sh` and `tests/run_vm_matrix.sh`, so log-constrained or AI-assisted runs can suppress passing integration-test chatter, replay the full labeled stdout/stderr for failing tests, print the final failure summary, and still emit compact completed-test liveness lines for non-failing tests.
-Switch the FreeBSD local qemu seed from a raw user-data script to a documented `#cloud-config` bootstrap that provisions both the default cloud user and root SSH key paths, enables root SSH login explicitly, restarts `sshd`, and waits for the temporary seed HTTP helper to exit cleanly so macOS hosts stop reporting spurious `Terminated: 15` noise during guest cleanup.
-Preserve ssh/bootstrap stderr for remote restore-mode backup metadata probes and report a transport-specific restore failure instead of collapsing host-key, authentication, and managed-ssh policy errors into the generic unreadable-backup message.
-Preserve ssh/bootstrap diagnostics for remote capability, uname, helper-path, and remote `--version` probes instead of collapsing transport/authentication failures into blank or generic dependency-query errors, so host-key, authentication, and managed-ssh policy problems now surface directly during remote helper discovery.
-Make chained live `-k` primary metadata writes and their forwarded-provenance destination-tree aliases commit as one transaction, so alias-write failures now roll back the primary file instead of leaving a durable primary-only state that can lose original-source provenance on the next backup hop.
-Switch local FreeBSD qemu guests from the Ubuntu-style `nocloud-net` SMBIOS seed path to an attached `cidata` config-drive with a FreeBSD-specific first-boot SSH provisioning script, so the official BASIC-CLOUDINIT images on Apple Silicon and other hosts now reach SSH readiness instead of stalling forever at the login prompt.
-Add `--jobs N` and `--stream-guest-output` to `tests/run_vm_matrix.sh`, keep serial execution as the default, log each major guest phase during local runs, and suppress macOS tar provenance xattr noise when copying the checkout into disposable guests.
-Improve `tests/run_vm_matrix.sh` setup visibility by logging guest-image cache/download/base-image preparation phases, adding SSH-wait heartbeats, using an interactive curl progress bar on serial local downloads, fixing parallel guest PID capture so setup logs are not swallowed, and forcing Darwin archive copies to disable macOS metadata/xattrs so `LIBARCHIVE.xattr.com.apple.provenance` warnings stop leaking into guest untars.
-Teach `tests/run_vm_matrix.sh` to shut down cleanly on `Ctrl+C` or `SIGTERM` by signalling active guest workers, waiting for backend cleanup, and removing temporary runner state instead of leaving local QEMU runs half-torn-down.
-Make `zxfer_transfer_properties()` consume derived override/create property lists through current-shell scratch state instead of an unchecked `$(zxfer_derive_override_lists ...)` here-document, so derivation failures now abort the property pass instead of silently dropping operator overrides or create-time property handling.
-Fix `tests/run_vm_matrix.sh` Ubuntu cloud-image checksum parsing so GNU `SHA256SUMS` entries that prefix binary files with `*` no longer fall back to the first unrelated manifest hash on macOS/arm64 and other hosts.
-Fail closed when live `-D` progress templates that use `%%size%%` cannot derive a valid size estimate, so exact estimate probe failures no longer get swallowed into an empty `-s` argument in the rendered progress wrapper.
-Add `tests/run_vm_matrix.sh` plus `tests/vm/` backend/guest drivers so unattended integration now runs through a backend-driven VM matrix for local disposable guest paths, with local QEMU orchestration on Linux/macOS/WSL2 and optional `ci-managed` entrypoints for guest-local CI-style execution. Apple Silicon and other arm64 hosts now prefer official arm64 Ubuntu and FreeBSD cloud images where available, while OmniOS remains pinned to the amd64 guest image.
-Preserve deferred pre-seed `-k` backup rows through post-seed property reconciliation instead of overwriting them with the later live source properties, so chained backups that pass through an intermediate destination now keep the original upstream restore provenance.
-Treat missing destination properties as needing a local set whenever the source side requires one, so `-P` plus escaped-comma `-o` overrides now still apply user properties even when the destination has never exposed that property before.
-Accept GNU `parallel` version probes even when citation or warning text precedes the GNU banner, so remote `-j` source discovery no longer rejects valid origin-host installs whose `--version` output is not headed by `GNU parallel` on the first line.
-Stop backup-metadata owner helpers from consuming mutator/rekey results through unchecked command substitutions, so buffered `-k` rows and forwarded provenance renders now fail closed instead of silently collapsing to empty state or header-only alias payloads when an inner helper throws.
-Teach chained `-k` backup runs to forward original-source property provenance through intermediate backups by writing a destination-tree metadata alias and reusing that exact-pair provenance on the next hop, so later `-e` restores from the final backup no longer record intermediate override or local-property drift as the "original" state.
-Record full backup-run `#source_root` and `#destination_root` dataset identifiers in `-k` metadata headers instead of the old ambiguous source tail plus destination-root labels, so operators inspecting recursive or nested backup files can tell which dataset tree the header describes.
-Confirm the resolved origin-host GNU `parallel` helper with a one-time remote `--version` probe only after adaptive `-j` discovery actually selects the per-dataset branch, so non-GNU implementations now fail closed without reintroducing a local `parallel` requirement for remote-only discovery.
-Require `-e` restore-mode backup metadata to carry the zxfer header plus a dedicated `#format_version:1` schema marker before any exact source/destination row is consumed, and keep that schema marker separate from the human-facing zxfer release `#version` line so future backup-format changes can fail closed without conflating release upgrades with on-disk format breaks.
-Make `-n` a strict no-exec preview by skipping launcher ssh preflight, remote helper/path validation, live backup-restore and unsupported-property checks, snapshot discovery, and `%%size%%` progress probes; dry runs now only preview the non-live-safe preflight work instead of validating against current source or destination state.
-Flush live `-k` backup metadata after each completed dataset flow instead of waiting for the final launcher epilogue, so later dataset failures no longer discard already-captured backup rows while seeded destinations still wait for their deferred post-receive property reconcile before becoming durable. Dry-run output keeps the existing one-shot preview behavior.

2026.04.09
-Make `-U` unsupported-property discovery probe destination support with direct `zfs get property` checks against the current source property names instead of comparing pool-root property presence, so valid user or dataset-specific properties are no longer dropped just because they are absent on the destination root at probe time.
-Escape raw ASCII control bytes in structured failure-report values before writing them to stderr or `ZXFER_ERROR_LOG`, so terminal control sequences embedded in `invocation`, `last_command`, or other report fields are rendered inert instead of being replayed.
-Add `ZXFER_REDACT_FAILURE_REPORT_COMMANDS` so failure reports can redact `invocation` and `last_command` on stderr and in `ZXFER_ERROR_LOG` mirrors when wrapper arguments or custom hooks may carry secrets, while keeping the default report format verbatim for compatibility.
-Add a zxfer-managed ssh transport policy that defaults remote connections to `BatchMode=yes` plus `StrictHostKeyChecking=yes`, supports an explicit `ZXFER_SSH_USER_KNOWN_HOSTS_FILE` override, and requires `ZXFER_SSH_USE_AMBIENT_CONFIG=1` to fall back fully to ambient ssh trust settings.
-Run remote backup-directory preparation plus remote `-k`/`-e` backup-metadata guard and staging helpers under the validated secure `PATH`, so auxiliary ownership/permission checks no longer trust the ambient remote shell environment.
-Reject relative `ZXFER_BACKUP_DIR` overrides so secure backup metadata can no longer follow the caller's or remote shell's current working directory; the backup-root override must now be an absolute path.
-Make `ZXFER_SECURE_PATH` authoritative for the live runtime `PATH` instead of silently merging the built-in system directories back in, so later bare helper invocations cannot escape an explicit secure-path override.
-Stage `ZXFER_ERROR_LOG` appends plus local and remote `-k`/`-e` backup-metadata reads and writes through same-directory snapshot/temp paths and final renames, closing the old validate-then-reopen race while tightening `ZXFER_ERROR_LOG` to require a pre-existing trusted parent directory owned by root or the effective UID, or protected by the sticky bit when writable by others.
-Teach `-o` property overrides to accept literal commas inside one value when the comma is escaped as `\,` in a quoted option argument, while keeping raw `=` and `;` handling unchanged after the first separator.
-Route the integration harness's exact-dataset snapshot comparisons through a recursive `zfs list -r -t snapshot -o name` helper filtered back to the requested dataset, so OmniOS no longer rejects those checks with `missing '@' delimiter in snapshot name` during idempotence and property-only assertions.
-Install Ubuntu `zfsutils-linux` in the VS Code / GitHub Codespaces devcontainer so contributors get `zfs` and `zpool` userland commands for local path-resolution and command-presence work, while keeping the docs explicit that the container still does not provide a live ZFS-capable kernel for the integration harness.
-Add a VS Code / GitHub Codespaces `.devcontainer/` based on Ubuntu 24.04, build upstream `kcov` there from a pinned release because Noble does not ship a native package, preinstall the pinned lint toolchain without linting the repository during image build, and extend `tests/run_lint.sh` with Linux arm64 asset pins so local Apple Silicon containers can reuse the same bootstrap path.
-Split the OmniOS GitHub Actions shunit2 and integration coverage back into separate VM jobs after the combined lane failed to reduce wall-clock time enough to justify the loss of failure isolation, keeping the shunit lane on a `bash --posix` wrapper and the integration lane on `/usr/xpg4/bin/sh`.
-Set `PKG_SUCCESS_ON_NOP=1` for the OmniOS GitHub Actions guest package installs so no-op `pkg install bash` or `pkg install zstd` operations return success instead of exit status 4 and no longer abort the OmniOS CI lanes before tests run.
-Drop the unused `bash` package install from the FreeBSD GitHub Actions integration guest, keeping only the dependencies that lane actually uses for `-j` and compressed replication coverage.
-Run the GitHub Actions lint, unit, coverage, and integration workflows on `push` only, removing separate `pull_request` triggers so same-repo branch updates schedule each workflow once per pushed ref instead of queueing a duplicate run that is then canceled by concurrency.
-Move runtime foreign scratch resets behind owner helpers for snapshot discovery/reconcile, send/receive, backup metadata, replication, and property state, so `zxfer_init_globals()` remains the startup entrypoint without centrally owning every mutable module variable.
-Split snapshot replication into clearer internal stages for live destination recheck, empty-destination seeding, final send/receive execution, and deferred post-seed property reconciliation, while keeping parsed `g_option_*` state immutable during runtime and making the seed-only receive `-F` path explicit.
-Move unsupported-property derivation into the property-reconcile module and route `-k` backup accumulation through backup-metadata owner helpers, tightening module ownership without changing CLI behavior or backup file output.
-Move readonly-property defaults out of mutable runtime globals into property-reconcile-owned `ZXFER_*` constants with getter helpers, stop migration flows from rewriting shared readonly state, and rename the yield-iteration ceiling to `ZXFER_MAX_YIELD_ITERATIONS` so internal naming now better reflects mutability without changing CLI behavior.
-Remove legacy backup-metadata restore compatibility so `-e` now reads only the current keyed secure files under the source-dataset-relative `ZXFER_BACKUP_DIR` tree, and document old metadata layouts plus mixed-version installs as unsupported.
-Refactor `zxfer_init_globals()` and `zxfer_init_variables()` into smaller internal helpers while keeping their public internal entrypoints and loader order stable, so startup responsibilities are easier to read and test without changing CLI behavior.
-Stop relying on source-time property scratch-global initialization by routing startup and iteration reset through the owning property-cache and property-reconcile helpers, reducing duplicated reset inventories and keeping module state ownership explicit.
-Clarify contributor docs around flat purpose-based `src/` module naming and module-owned reset/state boundaries, documenting that generic names such as `common`, `globals`, `utils`, and `lib` remain out of bounds.
-Add `docs/cli-examples.md` as a single CLI cookbook covering every current option with copy-and-edit command examples, and link it from the main documentation indexes.
-Refresh the testing guide, architecture notes, restore-mode manpage wording, troubleshooting guidance, and committed bash-xtrace coverage baseline so contributor docs and coverage-policy artifacts match the post-refactor backup-metadata and replication flow.
-Allow a small hit-count tolerance in the bash-xtrace no-regression coverage gate so Linux/macOS shell-tracing jitter no longer fails CI when the committed minimums are still met and only a line or two of approximate tracing differs.
-Skip the pre-seed live destination existence recheck for the initial root dataset when snapshot discovery already proved the destination tree is absent, fixing OmniOS bootstrap failures caused by a second missing-dataset probe before the first receive.
-Fallback ssh control-socket cache entries to a shorter default temp root when a safe but long `TMPDIR` would exceed Unix-domain socket path limits, and update the shunit coverage to derive the selected cache root from the same source helper.

2026.04.07
-Defer source snapshot-list reversal and per-dataset source/destination snapshot-index construction until a later delete/common-snapshot consumer actually requests per-dataset records, so no-op discovery runs no longer pay that cache-building cost up front while destructive planning paths retain the same indexed record access.
-Make adaptive remote source snapshot discovery (`-O ... -j ...`) skip `zstd` around metadata listing even when `-z` is enabled, while leaving real send/receive stream compression behavior unchanged, reducing no-op and startup-bound remote wall-clock time without weakening stream compression.
-Extend `-V` profiling output with accumulated millisecond stage timings for ssh setup/control-socket reuse, source snapshot listing, destination snapshot listing, and diff/sort work so no-op and startup-heavy runs can be compared more directly during performance tuning.
-Fail closed when `-m` source mounted-state probes or `-g` destination creation-time probes fail, so migration preflight and grandfather protection now surface the underlying metadata lookup failure instead of misreporting the source as unmounted or the snapshot as an ancient 1970-era candidate.
-Validate caller-supplied `TMPDIR` before using it for zxfer scratch files, temporary queues, or progress FIFOs, fall back to the system temp root when the override is relative, owned by another UID, or writable by other users without sticky-bit protection, and build FIFOs inside private `mktemp -d` directories so temp roots no longer trust unsafe parent directories or reopen race windows by unlinking and recreating FIFO paths in place.
-Prefer validated memory-backed default temp roots such as `/dev/shm` or `/run/shm` when `TMPDIR` is unset or rejected, fall back to disk-backed `/tmp` when no RAM-backed root qualifies, and route snapshot indexes, property caches, ssh control-socket caches, and remote capability caches through the same effective-tempdir helper used by ordinary scratch files and FIFOs.
-Honor dry-run semantics for `-n -k` by previewing local or remote secure backup-directory preparation commands instead of creating or chmodding `ZXFER_BACKUP_DIR` during preflight.
-Keep migration service relaunch state asserted until every queued `svcadm enable` succeeds, continue attempting later services after individual enable failures, and leave only the failed services queued for trap-exit recovery so `-m -c ...` no longer abandons remaining restarts after the first failure or loses the signal that services still need recovery.
-Reject symlinked parent components while preparing local and remote backup directories, including relative `ZXFER_BACKUP_DIR` paths, reusing the same whole-path symlink policy already applied to `ZXFER_ERROR_LOG` so `mkdir -p` and `chmod` no longer follow redirected `ZXFER_BACKUP_DIR` ancestors.
-Reject symlinked parent components while reading local and remote backup metadata, so restore-mode backup-file reads now fail closed before `cat` whenever any path component under `ZXFER_BACKUP_DIR` or a compatibility fallback path is a symlink.
-Harden backup-metadata compatibility path handling by switching mountpoint-derived secure helper paths to collision-resistant encoded components, preserving reads from the older sanitized mountpoint layout, and rejecting raw mountpoint fallback paths that would escape ZXFER_BACKUP_DIR.
-Require `-e` restore-mode backup metadata to match one exact source/destination row before restoring properties, so same-source metadata for other destinations and duplicate exact-pair rows now fail closed instead of silently restoring or concatenating the wrong property set.
-Add a collision-resistant source/destination-derived suffix to secure `-k` backup metadata filenames, so distinct replication runs no longer overwrite each other just because they share the same source tail, while `-e` still restores older tail-only backup files as a compatibility fallback.
-Key secure `-k` backup writes and `-e` restores from the same source-dataset-relative path under `ZXFER_BACKUP_DIR`, so matching backup runs no longer depend on destination mountpoints or broad whole-tree fallback scans to find their own metadata.
-Align remote `-k`/`-e` backup-metadata handling with the hardened local path by accepting secure remote metadata owned by either root or the ssh user, searching `ZXFER_BACKUP_DIR` remotely with the same ambiguity-checked fallback used locally, and resolving the target-side remote backup write helper through the secure dependency lookup path instead of invoking bare `cat`.
-Escape property values containing raw commas, equals signs, or semicolons throughout the live property-transfer and `-k` backup-metadata paths by storing an encoded internal representation and decoding only at the final `zfs create` / `zfs set` execution boundary, so delimiter-heavy share options and user properties no longer get split, truncated, or restored incorrectly.
-Fail closed when `-U` capability discovery cannot retrieve the source or destination pool property list, so transient `zfs get -Ho property all` probe failures now abort unsupported-property filtering instead of silently collapsing into an empty destination capability set that strips the entire property-transfer list.
-Resolve local and remote compression helpers through the same secure-PATH validation model used for other dependencies, and reuse the validated `-Z` compression/decompression commands for remote source snapshot discovery as well as the main send/receive pipeline so remote listing no longer depends on bare remote `zstd` from the login shell `PATH`.
-Coalesce concurrent remote capability handshakes across sibling zxfer processes with a secure per-host single-flight lock around the cached ssh capability probe, so simultaneously launched `-O`/`-T` runs now wait briefly for the first successful handshake to populate the hardened cache instead of stampeding the same remote helper-discovery round trip.
-Defer recursive snapshot GUID retrieval until identity validation actually needs it by starting source and destination discovery with name-only snapshot lists plus name-only per-dataset indexes, then lazily fetching guid-bearing records only for datasets whose overlapping snapshot names require common-snapshot, delete, rollback, or live-recheck identity validation, reducing remote discovery output and temp-file churn on deep mostly-no-op trees without weakening guid-based safety.
-Skip per-dataset delete/common-snapshot inspection for unchanged datasets during recursive `-d` runs by tracking destination-only snapshot delta datasets up front and iterating only the union of source-delta, destination-delta, and property-reconcile datasets, reducing no-op and delete-light planning overhead on large trees without weakening deletion safety.
-Reuse ssh control sockets across sibling zxfer processes through a validated per-user cache under `TMPDIR`, with compact cache/socket naming, per-process lease files, and stale-lease pruning so concurrent `-O`/`-T` runs against the same host can reuse a live ssh transport without racing teardown, redoing per-process control-socket bootstrap, or tripping typical Unix-domain socket path limits.
-Avoid exact `zfs send -nPv` progress-size probes when `-D` does not use `%%size%%`, and prefer cheaper approximate full/incremental size lookups for remote or multi-job progress runs with exact fallback when those probes are unavailable, reducing progress startup latency without changing replication behavior.
-Replace the remaining small property-diff helper loops with `awk`-backed transforms for override validation, override/create-list derivation, destination property diffing, child inherit adjustment, and unsupported-property filtering, reducing shell process churn in the post-discovery property path without changing transfer semantics.
-Batch destination snapshot creation-time reads per dataset during delete planning, caching the numeric `creation` results for rollback-eligibility and grandfather checks so deletion-heavy local and remote runs no longer issue one tiny `zfs get creation` call per candidate snapshot while still keeping the human-readable creation-time lookup only on the actual grandfather-protection error path.
-Make `-j` source snapshot discovery adaptive by counting recursive datasets once, choosing between the single recursive snapshot list and the older per-dataset GNU `parallel` fan-out before validating `parallel`, biasing warm remote startups more aggressively toward the per-dataset branch, and inlining prefetched moderate-size dataset lists into the chosen parallel pipeline so startup-bound local and multi-host remote runs avoid the old mandatory validation plus the extra recount pass before any data moves.
-Prefetch recursive source and destination property trees once per iteration during recursive property-transfer runs, slice the cached normalized results locally per dataset, and reuse that prefetched destination state for parent-effective-property checks so large `-P`/`-o`/`-k` runs avoid most first-hit per-dataset `zfs get` round trips.
-Build per-dataset source and destination snapshot indexes once during snapshot discovery, preserving source newest-first order and destination live order so recursive delete/common-snapshot planning no longer rescans the full global snapshot lists for every dataset pass.
-Collapse remote startup helper discovery into one per-host capability handshake, cache the parsed `uname`/`zfs`/`parallel`/`cat` results for the current zxfer process plus a short-lived per-host cache file under `TMPDIR`, and let `prepare_remote_host_connections()` prewarm that state so repeated `-O`/`-T` startup probes no longer dominate short or multi-host orchestrated runs.
-Replace the old `cat -n | sort -nr | cut` snapshot-list reversal with a bounded POSIX-awk fast path plus automatic sort fallback for larger inputs, reducing reversal work on typical snapshot inventories without making very large trees depend on unbounded awk memory.
-Stop rebuilding the full source/destination snapshot tree before deferred post-seed property reconciliation, instead updating seeded destination datasets in memory and clearing only the destination-side property cache so first-replication and bootstrap runs avoid an unnecessary full-tree refresh at the tail end of the iteration.
-Seed a per-iteration destination-existence cache from the recursive destination dataset list, reuse cached confirmed exists/missing states on non-destructive paths, and update the cache after successful creates and foreground receives so remote targets avoid redundant `zfs list -H` probes without weakening the existing fail-closed live rechecks for rollback and live snapshot reconciliation.
-Replace shell-string snapshot membership scans in `get_last_common_snapshot()` and live destination rechecks with `awk`-backed identity-set lookups, keeping the existing guid-based matching and newest-common-snapshot semantics while reducing repeated large-string scans on deep histories.
-Replace the `-j` send/receive batch barrier with a rolling completion-based worker pool, so zxfer now starts the next eligible transfer as soon as any in-flight job finishes, while still failing fast on the first non-zero background send/receive exit and terminating the remaining jobs.
-Resolve destination-only snapshot deletes in one destination-order-preserving pass after the identity `comm` diff, removing the old O(deletes * destination_snapshots) rescan in `get_dest_snapshots_to_delete_per_dataset()` and keeping grandfather/rollback-visible delete ordering aligned with the live target.
-Batch per-dataset `zfs set` updates during property reconciliation so recursive `-P` and `-o` runs now execute one destination-side `zfs set prop=value ... dataset` call per dataset instead of one round trip per property, while keeping `zfs inherit` behavior unchanged.
-Cache raw normalized property reads and explicit required-property probes per replication iteration, keying source and destination lookups separately and invalidating destination-side entries after create, receive, set, and inherit operations so recursive property reconciliation avoids redundant `zfs get` work without changing CLI behavior.

2026.04.06
-Add `-V` profiling counters that report end-of-run baseline metrics for source/destination `zfs` calls, ssh shell invocations, snapshot-list/send-receive pipelines, destination-existence probes, and property hot-path reads so optimization work can be measured before and after changes without affecting normal output modes.
-Make dry-run `-n -s` and `-n -m` render-only for source-side preflight work, so zxfer now previews migration service disables, re-enables, unmounts, and snapshot creation without executing them or refreshing cached snapshot state as though those actions had already happened.
-Turn shell coverage from an informative report into an enforced gate by normalizing bash-xtrace reports to repo-relative paths, appending a `TOTAL` row, checking committed per-file minimums plus total/module no-regression baselines, publishing `missing.txt` diffs into the GitHub Actions step summary, and checking in the baseline artifacts under `tests/coverage_baseline/bash-xtrace/`.
-Fail closed when source dataset type or zvol `volsize` probes fail during property transfer, so zxfer no longer falls through to a filesystem create when the source was really a volume with missing metadata, and refuse direct volume creates without a validated size in the helper path.
-Fail closed when creation-time must-create properties are missing from `zfs get all` and their explicit follow-up probes fail unexpectedly, while still tolerating clearly non-applicable/unsupported properties, so property transfer no longer skips `casesensitivity`/`normalization`/`utf8only` mismatch guards on probe errors.
-Match source and destination snapshots by `guid` identity instead of trusting `@name` alone, carrying `name,guid` records through snapshot discovery, delete planning, live destination rechecks, and common-snapshot selection so same-named but unrelated snapshots no longer survive `-d`, anchor rollback, drive invalid incremental sends, or trigger full receives into existing snapshotted datasets when no real common base remains.
-Fail closed when destination existence checks or live destination snapshot rechecks fail, so ssh, permission, and other operational probe errors no longer collapse into “dataset absent” or “existing dataset has no snapshots” and trigger the wrong seed/rollback/create-parent path.
-Add adversarial shunit2 regressions for delimiter-heavy property values, wrapper-style host specs, odd `TMPDIR` and ssh control-socket paths, and awkward dataset tails so quoting and backup/restore serialization paths are exercised against the highest-value edge cases called out in `KNOWN_ISSUES.md`.
-Replace the ad hoc GitHub Actions lint-tool installs with a shared `tests/run_lint.sh` bootstrap that pins `actionlint`, `checkbashisms`, `shfmt`, `codespell`, and ShellCheck by exact version and SHA-256, use that same script for local lint runs and CI matrix jobs, pin the `kcov/kcov` coverage image by digest, and move the bash-xtrace coverage lane to `ubuntu-24.04` for tighter CI reproducibility.
-Stop interpolating `TMPDIR`-derived temp file paths into `eval`-built snapshot normalization and source-list sorting commands, switching those flows to direct argv execution so metacharacter-bearing temp directories no longer collapse into invalid paths or execute local substitutions.
-Track zxfer-owned background PIDs explicitly for `trap_exit()` cleanup instead of killing every direct child of the shell, preventing bash-xtrace coverage helpers and similar caller-owned children from being terminated as status 143 while still cleaning up snapshot-list, progress, delete-planning, and background send jobs.
-Split the GitHub Actions coverage workflow into a stable `bash-xtrace` lane and a separate Docker-backed `kcov` lane on Ubuntu, uploading distinct artifacts for each so CI now captures child-shell and launcher coverage without depending on a runner-installed `kcov` package.
-Remove the string-based ssh transport execution path for remote helper and snapshot-list commands, keep remote `g_LZFS`/`g_RZFS` as resolved `zfs` paths instead of composite ssh snippets, quote control-socket paths via argv-derived renderers, and add shunit plus manual integration regressions for shell-metacharacter-bearing control-socket paths derived from `TMPDIR`.
-Teach `tests/run_shunit_tests.sh` to honor `ZXFER_TEST_SHELL` so CI and local developers can rerun the existing shunit2 suites under alternate interpreters without rewriting suite shebangs, add runner regressions for the new dispatch path, and expand the GitHub Actions unit workflow with Ubuntu lanes for `dash`, `bash --posix`, `busybox ash`, and an initially non-blocking `posh` pass.
-Add a dedicated GitHub Actions `checkbashisms` lint job, running in POSIX mode against the tracked `/bin/sh` scripts and the top-level `zxfer` launcher while excluding the vendored `tests/shunit2/shunit2` harness, so CI now has an explicit bashism gate alongside ShellCheck and shfmt.
-Route local and remote helper paths through shell-quoted token streams instead of raw command-fragment concatenation, using resolved origin/target `zfs` paths for send/receive, argv-safe remote `uname` detection, and validated local/remote dependency paths so helper lookups with metacharacters or control whitespace no longer re-enter `eval`-executed pipelines.
-Validate optional local GNU `parallel` with the same resolved-path checks as required helpers, closing the remaining `ZXFER_SECURE_PATH`-driven snapshot-listing helper gap.
-Add shunit2 regressions for malicious resolved local/remote `zfs`, `cat`, and `ssh` helper paths plus manual integration-harness regressions that resolve `ssh`/`zfs` from directories whose names literally contain `$(touch ...)`, ensuring local and remote runs succeed without creating the marker files.
-Update the GitHub Actions artifact-upload steps to `actions/upload-artifact@v6`, which runs on Node 24 by default, so the new GitHub-hosted runner deprecation warning for Node 20 no longer fires on the coverage and integration workflows.
-Fix the coverage workflow to set `COVERAGE_DIR` from `runner.temp` at step scope instead of job scope, matching where the GitHub Actions `runner` context is actually available and preventing workflow-parse failures on GitHub.
-Fix the GitHub Actions `actionlint` job by installing the pinned upstream Linux release binary directly, since the `rhysd/actionlint` repository does not publish a `v1` GitHub Action ref and newer `actionlint` releases can require a newer Go toolchain than the runner job was setting up.
-Preserve the integration harness workdir on CI failures when `ZXFER_PRESERVE_WORKDIR_ON_FAILURE=1` is set, and upload the failed Ubuntu GitHub Actions integration workdir as an artifact so logs, mock outputs, and captured stderr/stdout files remain available for debugging.
-Add `actionlint` to the GitHub Actions lint workflow so workflow YAML and expressions are validated in CI alongside ShellCheck, shfmt, and the existing repository-hygiene checks.
-Add a dedicated GitHub Actions coverage workflow on `ubuntu-latest` that runs `tests/run_coverage.sh` in bash-xtrace mode, uploads the resulting coverage directory as an artifact, and use workflow-level concurrency cancellation so stale branch runs do not pile up across lint, unit, integration, and coverage jobs.
-Run the GitHub Actions shunit2 workflow as a matrix on `ubuntu-latest` and `macos-latest`, and document that hosted macOS remains a unit/smoke portability gate rather than a required ZFS integration gate while the current Darwin property caveats remain open.
-Add a launcher-side compatibility shim for `prepare_remote_host_connections()` so mixed-version installs do not emit a spurious `prepare_remote_host_connections: not found` error when only the top-level `zxfer` script was updated.
-Extend `examples/error-log-email-notify.sh` with a POSIX-safe whitespace-separated `SRC_DATASETS` list, running each source root sequentially with the existing alerting flow and stopping on the first failure.
-Expand the example self-test and add launcher shunit2 coverage for the mixed-version compatibility path, then refresh the README, troubleshooting guide, examples README, and both manpages to document the wrapper changes.
-Stop mutating `g_readonly_properties` inside `transfer_properties()` by building a per-call effective readonly-property list for platform-specific filtering, preventing recursive and `-Y` property passes from growing the list across datasets and iterations while preserving migration-mode `mountpoint` handling.
-Pass the effective readonly-property list explicitly through destination-creation and child-inherit helpers, and add shunit2 coverage so repeated FreeBSD/SunOS property transfers do not silently reintroduce list growth.
-Stop shell-escaping `zfs set` / `zfs inherit` argv before direct execution, preserving literal property names and values locally, and route remote `-T` property updates through a shell-quoted ssh command so assignments survive ssh's remote-shell command joining.
-Reverse source snapshot caches with a tab-field strip instead of `cut -c 8-`, preventing large recursive source lists from truncating snapshot names once `cat -n` line numbers grow past six digits.
-Pass `-x` exclusion patterns to `grep` via `-e` so leading-dash regexes work portably across supported platforms, and document anchored exact-match examples for dataset exclusion in the README and manpages.
-Standardize the remaining dry-run command rendering on the shared report formatter so `zfs create` and backup-metadata write previews use one quoting path, and remove the older double-quote-specific display helper.

2026.04.04
-Defer `-O`/`-T` ssh control-socket creation until after CLI consistency checks pass so usage errors like `-m` or `-c` with remote hosts fail closed without attempting a live ssh connection.
-Relax the FreeBSD beep integration assertion to accept either missing speaker helper tools or a missing `/dev/speaker`, matching legitimate host-specific skip paths.
-Generalize `examples/error-log-email-notify.sh` for Linux, illumos, FreeBSD, and macOS mail setups by checking `mailx`, BSD `mail`, and `sendmail`, exposing sender/remote/raw-send knobs, and preserving the original zxfer exit status if mail delivery itself fails.
-Add a built-in `--self-test` to `examples/error-log-email-notify.sh`, cover it from the integration harness, and refresh the example/troubleshooting/manpage docs so operators can validate alert delivery logic before using it against production pools.

2026.04.03
-Restore the local `-j` source snapshot-listing path to the direct GNU `parallel` runner used by `development`, fixing Linux background-listing regressions that showed up in job-limit, progress-wrapper, and background-send integration tests as `Failed to retrieve snapshots from the source: missing command`.
-Preserve the failing source snapshot-listing command in snapshot-discovery failure reports even after later destination `zfs list` calls run, and add unit coverage for the local `-j` direct GNU `parallel` runner so the regression cannot slip back in unnoticed.
-Force snapshot sorting and `comm` comparisons through `LC_ALL=C` in discovery and delete-planning helpers so macOS/BSD locale collation does not reorder dataset paths differently from Linux or destabilize shunit2 expectations.
-Run wrapped remote host specs like `host pfexec` / `host doas` through `sh -c` when building send/receive ssh pipelines so wrapper commands execute the intended remote pipeline instead of trying to `exec` the whole pipeline string as a binary.
-Treat remote `command -v` exit status `1` with empty output as a missing dependency rather than a probe failure, restoring the expected `GNU parallel not found on origin host` error path for remote `-j` validation.
-Prefer GNU `stat -c` before BSD `stat -f` when checking backup metadata ownership and permissions locally or remotely, fixing Linux/GitHub restore and remote-backup integration failures caused by GNU `stat -f` emitting filesystem metadata instead of numeric uid/mode values.
-Track zxfer temp files under a per-run prefix and remove the whole prefix set in `trap_exit`, fixing leaked `/tmp/zxfer.*` files after interrupted background-send integration runs.
-Gate the explicit pre-send `zfs rollback -r` path behind `-F` so `-d` no longer performs a destructive rollback unless the caller explicitly opted into forced receive behavior.
-Tighten restore-mode backup metadata fallback under `ZXFER_BACKUP_DIR` by requiring fallback files to match the requested source dataset and aborting on ambiguous basename collisions instead of choosing the first hit.
-Adjust the mismatch-deletion integration flow to use `-F` for resend-after-delete divergence cases, keeping the test aligned with zxfer's fail-safe rollback semantics.
-Expand shunit2 coverage for rollback gating, backup-metadata ambiguity, must-create property propagation, remote shell/probe behavior, and the newer failure-reporting paths.
-Add a dedicated GitHub Actions ZFS integration workflow, run the harness with `--keep-going`, and clean up the remaining ShellCheck/shfmt/CI issues in tests and helpers.
-Reorganize the repository layout by adding `docs/`, `examples/`, `man/`, `packaging/`, contributor/security docs, issue/PR templates, and `CODEOWNERS`, and move the manpages/spec/plaintext compatibility README into the new structure.
-Refresh `README.md`, `AGENTS.md`, the manpages, and project docs so they match the current source tree, secure-PATH model, integration harness behavior, and testing/CI workflow.
-Add `.gitignore` for local/editor/test artifacts and clean up stale documentation like `TODO_Tests.md`/`README.txt` by merging the still-relevant parts into the canonical docs.
-Remove stale README merge debris, fix the backup/restore manpage example path mismatch, and align the RPM spec with the current `zxfer` name plus optional `parallel`/`zstd` feature dependencies.
-Clarify README/platform documentation so remote helper hardening claims match the current `KNOWN_ISSUES.md` scope, note the macOS caveat inline, and make the RPM spec more portable by replacing the fixed `/sbin/zfs` dependency with a generic `zfs` requirement.

2026.04.02
-Make the integration harness safer and more usable by default: confirm data-modifying wrapped commands unless `--yes` is passed, add `--skip-test`, and add `--keep-going` failure collection.
-Harden the macOS/OpenZFS integration harness by avoiding unnecessary destination mounts, fixing mock helper overwrites/ssh argv handling, and improving diagnostics around flaky snapshot assertions.
-Fix multiple property-transfer edge cases around seeded or pre-created destinations, must-create property validation, override handling, unsupported-property filtering, and post-seed property reconciliation.
-Fix remote command and snapshot-listing regressions for real `ssh` command joining, wrapped host specs, and `csh`/`tcsh` remote shells, including better remote dependency probing and clearer failure reporting.
-Add more end-to-end coverage for remote property backup/restore, existing-empty destination seeding, secure-path append resolution, progress placeholder passthrough, target-only remote compression, legacy backup fallback warnings, and exact-name snapshot deletion safety.
-Clean up shellcheck/shfmt issues across the source and test suites and add CI parity checks for the updated shell/test workflow.

2026.04.01
-Stop after seeding the first snapshot into an empty destination when that seed is already the newest snapshot, avoiding invalid `zfs send -I snap snap` streams during first-run replication, property backup/restore tests, trailing-slash/exclude tests, and legacy backup fallback flows.
-Recheck live destination snapshots before reseeding an existing dataset during `-Y -d` iterations so destination-only snapshot cleanup does not trigger a second full receive into a dataset that still has the last common snapshot.
-Wrap the remote `-O ... -j ...` snapshot-listing pipeline in `sh -c` before it reaches the origin host's login shell, fixing `csh` quote parsing failures that showed up as `Unmatched '"'`, broken `zstd` streams, and `Failed to retrieve snapshots from the source`.
-Make `tests/run_integration_zxfer.sh` prompt for approval before data-modifying wrapped external commands by default, and add `--yes` to run the harness without confirmations.
-Resolve `zfs` (and remote `cat` for `-e`) on origin/target hosts through the secure PATH so mixed Linux/FreeBSD/Solaris replication no longer assumes the local absolute binary path exists remotely.
-Probe remote GNU `parallel` via the secure PATH using the host-spec tokenization helper so `-O "host pfexec ..."` remains portable and safe.
-Emit a detailed failure report to stderr on every non-zero exit, including failure class/stage, source and destination context, the original shell-quoted invocation, and the last attempted command.
-Add optional secure failure-report mirroring via `ZXFER_ERROR_LOG`, refusing relative paths, symlinked path components, and insecure existing log files while preserving the original zxfer exit status if the log append fails.
-Harden startup argument capture by forcing the bootstrap PATH to the built-in allowlist before any early awk/sed helpers run, preventing PATH-injected binaries from executing before secure-path initialization.
-Convert remaining ad hoc fatal exits in migration/property/backup flows to the centralized error-reporting path and add shunit2/integration coverage for stderr reporting and error-log behavior.
-Add `tests/run_coverage.sh` with `kcov` support plus a bash-xtrace fallback, and expand shunit2 coverage for globals, snapshot-listing, zfs-mode, and send/receive helpers.

2026.03.31
-update copyright years

2025.12.18
-Fix property restore lookup to match backup file format (source,destination,properties), with a fallback for legacy order.
-Create ssh control sockets with -fN before the host spec so options are parsed correctly and remote commands are not invoked.
-Fix remote backup metadata prep/validation commands to avoid escaped command substitutions and keep remote scripts one-line for ssh on FreeBSD.
-Fix remote backup metadata writes to avoid double-escaping quotes so directories are created and files can be written on FreeBSD.
-Treat missing destination datasets as empty listings during get_zfs_list so bootstrap restores do not fail on new targets.
-Only treat missing destination datasets as empty when the destination pool exists, keeping the missing-pool error behavior intact.
-Create parent datasets when needed for initial destination creation so restores can bootstrap into new targets.

2025.12.12
-When zxfer pre-creates an empty destination for property transfer (-P/-k), seed it with a forced full receive (-F) so older OpenZFS versions accept the initial stream while preserving must-create properties.
-Fix property backup (-k) collection to persist in the parent shell so backup metadata files are actually written during runs.

2025.11.20
-add integration tests
-Wait for `-d` snapshot deletions to finish and roll the destination back to the
 last common snapshot before sending incrementals so mismatched snapshot names
 no longer trigger `zfs receive ... has been modified since most recent snapshot`
 failures mid-run.

2025.11.19
-Ensure `-d` deletion passes walk every dataset even when no new snapshots are pending so orphaned destination snapshots are proposed/destroyed during dry-run and live zxfer runs, and cover the copy loop behavior via new shunit2 tests.
-Add shunit2 coverage for quoting helpers, backup directory sanitizers, snapshot deletion guards, property diff/apply logic, snapshot listing orchestration, and zfs send/receive plumbing, and strip the trailing comma from batched destroy commands so `zxfer -d` emits valid `zfs destroy dataset@snap1,snap2` arguments.

2025.11.18
-Fix ssh control socket initialization to create temp directories with mktemp-compatible templates (suffix `XXXXXX`) so Linux and GNU coreutils builds no longer abort zxfer or the shunit2 probe when -O/-T opens a control socket.
-Add `tests/run_shunit_tests.sh` to execute every shunit2 suite (or a specific subset) from one command so contributors can easily exercise helpers like `test_zxfer_replication.sh` without remembering individual scripts.
-Expand `tests/test_zxfer_replication.sh` coverage for `set_actual_dest()`, `refresh_dataset_iteration_state()`, grandfather protection, and the `-Y` run loop so option parsing and migration orchestration stay regression-tested.
-Refactor snapshot listing orchestration [`write_source_snapshot_list_to_file()`, `write_destination_snapshot_list_to_files()`, `set_g_recursive_source_list()`] into data-flow helpers that build the source command string, normalize destination prefixes, and diff sorted lists so background jobs no longer embed inline eval plumbing, enabling shunit2 coverage of ssh/parallel/compression permutations without touching live ZFS.
-Quote the per-dataset GNU parallel runner with literal single quotes so remote shells that default to `/bin/csh` no longer emit repeated `Unmatched '"'` errors when `-O` and `-j` are used together, stop escaping the `$1` placeholder so dataset arguments reach the per-dataset `zfs list`, and add shunit2 coverage to guard the new quoting.
-Refactor `transfer_properties()` into composable helpers (source property collection, override derivation, destination diff/apply) and add shunit2 coverage with injectable set/inherit runners so safety-sensitive property logic can be unit-tested without touching real pools.
-Allow zxfer to treat backup directories and metadata files owned by the invoking user as secure when root privileges are unavailable (while still enforcing 0700/0600 permissions) so shunit2 and CI runs on Linux can exercise the helpers without failing ownership checks.
-Run recursive property/backup passes even when every dataset is already in sync so -P/-o/-k still enforce metadata, and skip writing `.zxfer_backup_info.*` files when no property data was captured to avoid clobbering prior backups.
-Reset PATH to `/sbin:/bin:/usr/sbin:/usr/bin:/usr/local/sbin:/usr/local/bin`, resolve required binaries to their absolute paths, and teach zxfer to honor `ZXFER_SECURE_PATH[_APPEND]` so PATH hijacking is no longer possible when zxfer runs with elevated privileges.

2025.11.17
-Reject non-numeric `-j` arguments during option parsing so invalid job counts emit a zxfer usage error instead of `/bin/sh: integer expression expected` later on.
-Allow zxfer to bootstrap into brand-new destinations by tolerating an empty `zfs list -t filesystem,volume … "$g_destination"` result instead of aborting when no datasets exist yet on the target.
-Refuse to use zxfer backup metadata directories unless they are owned by root locally or by the remote ssh user (root also permitted) so attackers cannot pre-create `ZXFER_BACKUP_DIR` paths and retain ownership of `.zxfer_backup_info.*` files.
-Require property restores (-e) to read `.zxfer_backup_info.*` files only when they are root-owned and mode 0600 so compromised backup directories cannot poison mountpoint, sharing, or encryption metadata.
-Quote -O/-T ssh host specs before constructing eval'd pipelines and open/close ssh control sockets without eval so a crafted host argument can no longer inject local commands before ssh starts.
-Treat `-Z` strings as tokenized commands and refuse empty compression pipelines so attacker-controlled configs cannot splice arbitrary shell syntax into the send/receive ssh pipelines.
-Create zxfer progress FIFOs with umask 077 and chmod 600 so local users cannot read or stall the replicated send stream via /tmp/zxfer-progress.*
-Quote the progress passthrough hook with single quotes so remote `-O -D` pulls keep the ssh command coherent and progress works on origin hosts.
-Estimate incremental stream sizes when a previous snapshot exists so the progress dialog reflects the bytes that will actually be sent.
-Keep g_is_performed_send_destroy unset during dry-run sends so `-Y -n` exits after the first loop when no mutations occur.
-Eliminate command injection in transfer_properties() by building `zfs create` argument lists without eval so property values are passed verbatim even when the destination dataset does not yet exist.
-Fail replication immediately when any background `-j` zfs send/receive job exits non-zero by tracking their PIDs, surfacing failures, and terminating the remaining jobs instead of silently reporting success.
-Store `.zxfer_backup_info.*` metadata under a root-owned `ZXFER_BACKUP_DIR` (default `/var/db/zxfer`), harden directory creation against symlinks, and warn when falling back to legacy files so attackers can no longer pre-create mountpoint symlinks to clobber arbitrary files during `-k`.
-Ensure the -D progress hook tees the send stream so non-pass-through progress commands no longer truncate the data before `zfs receive`.
-Seed existing destination datasets that have no snapshots by sending the first snapshot before incremental streams so pre-created replicas keep their full history instead of only the newest snapshot.
-Gate the beep() helper on FreeBSD speaker support so Linux/OpenZFS hosts skip hardware beeps gracefully instead of aborting when -b/-B is set.
-Resolve GNU parallel via ssh for -j snapshot listings so remote -O hosts no longer reuse the local path and abort when the binary lives elsewhere, and refuse -j when the remote binary is missing.
-Eliminate command injection in write_backup_properties() by streaming raw property data directly into the backup file and expand shunit2 coverage for the helper
-Normalize destination snapshot prefixes for non-trailing-slash transfers so dataset detection matches pre-2025.11.14 behavior, while keeping trailing-slash multi-host replicas isolated.
-Strip trailing slashes from destination dataset arguments before replication so `zxfer -N tank/src backup/dst/` no longer builds illegal `backup/dst//child` targets that `zfs receive` rejects.
-Filter source/destination snapshot lists by exact dataset names when building replication plans so nested filesystems that share a basename (for example, trailing “/tank”) no longer collide when computing the last common snapshot.
-Wrap the GNU parallel snapshot listing commands in `sh -c` and pass dataset names as `$1` arguments so per-dataset jobs execute `/sbin/zfs list … "$1"` with proper argument splitting even when datasets contain slashes or spaces, fixing both local and remote `-O` listing.

2025.11.15
-Destination snapshot lookup now respects trailing-slash sources so incremental sends compare against the correct dataset
-Track fully qualified last-common snapshot
-Escape dataset names before sed substitution
-Fix GNU parallel snapshot listing
-Restore trailing-slash destination handling
-Literalize snapshot prefix matching
-Fix integration multi-dataset replication
-remove the legacy rsync (-S) mode and delete unused options and helpers
-drop the rsync dependency from zxfer.spec and refresh the man pages/README
-update integration tests and scripts now that rsync transfer paths no longer exist
-vendor shunit2 test harness and helper script
-add zxfer_common shunit2 smoke tests and make them executable
-document how to run the new shunit2 tests
-add GitHub Actions workflow to run shunit2 tests on pushes and PRs
-fix get_temp_file mktemp template to work on GNU/Linux CI runners
-exclude vendored tests/shunit2 from codespell checks
-add integration test script that provisions loopback ZFS pools for zxfer smoke tests
-add shfmt formatting gate to lint workflow
-multiple lint fixes
-Add lint workflow for codespell & ShellCheck
-fix shellcheck warnings
-source files for shellcheck
-add .shellcheckrc
-Fixed handling of filtered destination properties in transfer_properties
-Use argument in newsnap instead of global initial_source
-Fix -m migration guard to reject unmounted sources

2025.11.14
-spelling fixes
-copyright year update
-Secure ssh control socket creation
-Ensure -o overrides get applied in transfer_properties
-fix misspelling of throw_error in transfer_properties
-Handle local vs remote backup writes correctly
-Fix remote property restore lookup
-Resolve cat path correctly for local property restores
-Filter existing-destination property list correctly
-Manage ssh control sockets per host
-Fix property/rsync command injection
-update zxfer.spec file
-document zxfer.8 option restrictions and compression requirements
-sync zxfer.1m with zxfer.8

2024.09.20
-fix bug in transfer_properties() where the source dataset was not being properly set

2024.09.19
-add -x option to exclude datasets that match the given grep pattern

2024.08.04
-fix an issue with copying the first snapshot when a destination dataset doesn't
 exist

2024.08.03
-add very verbose output to display snapshot deltas in set_g_recursive_source_list()
-clean up copy_snapshots()

2024.08.01
-commented out parallel use for listing destination due to poorer performance
 when metadata cached
-add parallel support when listing destination snapshots

2024.07.29
-update zfs list for IllumOS to use '-d 1' when listing snapshots
-only pipe parallel output through zstd if compression is enabled
-add exists_destination() to check if destination dataset exists
-update trap_exit() to quit child processes and exit with proper exit code

2024.07.28
-add g_cmd_parallel variable to use which to locate path to parallel
-sort g_recursive_source_list to properly create datasets
-fix bug where if destination dataset does not exist, only the last snapshot
 is sent. Now, the first snapshot is sent and then the full snapshot range
 is sent.
-fix listing destination snapshots when pushing via ssh

2024.07.27
-parallelize the comparison of snapshots to deleted
-use global temporary files for snapshot deletion
-refactor zfs_get_list()
-code cleanup

2024.07.26
-use ssh control socket for all ssh connections

2024.07.24
-rename -x option to -j for number of parallel jobs

2024.07.23
-run zfs send operations as background processes (later limited to -j #jobs)

2024.07.21
-optimize grep calls by passing -m 1 (later removed for Illumos compatibility)

2024.07.16
-add use of gnu parallel to list source snapshots ordered by creation
-remove creation of one temporary file

2024.07.15
-add -x option to specify number of parallel jobs for zfs list

2024.07.12
-batch zfs destroy commands
-optimize zfs list on destination snapshots by removing sorting

2024.07.10
-optimize get_zfs_list() by only listing snapshots of the intended destination
 dataset instead of listing all the snapshots of the parent destination dataset

2024.04.09
-add consistency check for -Y option

2024.04.08
-add -Y (Y)ield command that runs run_zfs_mode up to a maximum of 8 times
 while there are zfs send or zfs destroy commands that are executed

2024.04.06
-fix bug in get_zfs_list() when 2 snapshots are taken at the same time
-optimize grep usage in set_last_common_snapshot

2024.04.03
-run zfs destroy as a background process

2024.03.31
-add option -w for zfs raw send

2024.03.19
-copy_snap_mulitple now sends one snapshot range and set_send_command
 switched from zfs send -i to zfs send -I

2024.03.14
-refactor script into multiple files, grouping files by function usage

2024.03.13
-remove unused variables in get_zfs_list() and clean it up
-prefix global variables with g_ and local variables with l_

2024.03.12
-refactor get_zfs_list() to run zfs list commands in the background to reduce
 the time it takes to get the list of filesystems
-add -V option for (V)ery verbose mode using echoV()
-optimize set_last_common_snapshot() by using grep instead of nested loops
-add echoV statements for debugging

2024.03.11
-refactor inspect_delete_snap() into multiple functions with the primary goal
 of optimizing the snapshot deletion logic. Convert that from nested loops
 to using temp files and comm for a more efficient comparison.

2024.03.09
-add -z option to use zstd compression
-add -Z option to specify the zstd compression command
-refactor of the code
-add execute_command() function
-add throw_error functions
-refactor copy_snap()
-rename option variables for readability
-wrap long blocks in functions to improve readability
-update verbose messages to display the command being executed
-group functions by mode

2024.03.06
-Fork of zxfer to totalAldo/zxfer

20110513 ver 0.9.9
           -Fixed a small but showstopping bug in Solaris that wasn't showing
	     up in FreeBSD. Thanks predrag....@googlemail.com. Because this
	     is the only fix and is not affecting FreeBSD, I may not update
	     the port until the next version.
20110510 ver 0.9.8
           -Added -c option to default rsync options list. Note that this
	     much increases reliability of backups, at the expense of speed.
	   -Removed --del option when transferring single files with -N.
           -Fixed bug in rsync transfer, where --del option caused
	     attempted deletes of filesystem mountpoints on destination.
           -Made the grandfather protection option check everything BEFORE
	     any deletions would occur. If grandfathers are being deleted,
	     chances are something is wrong, so better to fail before doing
	     damage.
	   -Added a "p" switch to persist in face of rsync errors. Needed
	     for a full system restore.
	   -Added an "E" to enable passing of excludes to rsync (useful
	     for avoiding transfer of zpool.cache)
	   -Added an "i" option to enable inclusion of filesystem mountpoints
	     on the destination in -S mode, as they needed to be excluded
	     by default.
	     If they aren't excluded then --del will wipe anything there,
	     e.g. contents from a filesystem from another pool.
20110407 ver 0.9.7
           -Removed the extra backup related content from the man page
             as more suitable for external documentation. KISS.
	   -Added in a "grandfather protection" option [-g], to prevent
	     grandfather snapshots being deleted inadvertently.
20110405   -Again, added to and modified the man page significantly, to
              explain tape vs HDD backup.
20110404   -Made zxfer more portable, by testing for the actual $PATH via
             "which" instead of guessing which directories the programs might
	     live in.
20110402 ver 0.9.6
           -Added a section "SNAPSHOTS" to the man page, to express that
             zxfer is designed to work with snapshots, and how it does so.
	   -Made zxfer use awk in FreeBSD, gawk in Solaris, so that there
	     is only the rsync dependency remaining for purpose of porting.
20110331 ver 0.9.5
           -Changed download package to have a directory within, to suit ports.
20110331 ver 0.9.4
           -Some refactoring, updating man page, and testing to make
             it work better on S11E.
20110329 ver 0.9.3
           -Tested local transfers on FBSD 8.2 and SE11. Tested remote
             transfers on FBSD 8.2.
	   -Updated the man page.
20110328   -Implemented the -O and -T remote transfer options. Tested on
             FreeBSD somewhat.
20110327 ver 0.9.2
           -Added "Compatibility" section to man page.
           -Added to the man page a warning not to tack switches with arguments
             on the end of a list of switches without arguments.
	   -Alphabetized the options in the man page, and in the script.
20110323   -Started fixing a bug so that zxfer can work with remote hosts via
             ssh.
	   -Added "vscan,nbmand" to the list of properties not supported
	     by FreeBSD.
20110321   -Added "xattr" to list of properties not supported on FreeBSD,
             so that zxfer works on FreeBSD 8.2 out of the box.
	   -Changed the beep option, breaking backwards compatibility for
	     the sake of having more logical switches. The B is a "big beep",
	     alerting the user for success or failure. The b is a "small beep"
	     that will only sound on error.
20101211 ver 0.9.0 -Added some output for errors.
           -Added proviso to man page about backing up/restoring FreeBSD
	     root directories.
           -Familiarized self with mercurial workflow in anticipation of
	     publishing.
           -Fixed several Solaris compatibility bugs.
           -Added /usr/bin as rsync location
	   -Changed awk to gawk
	   -mlslabel transferring is not supported at this stage.
	   -ditto for keysource,keystatus,rekeydate,encryption.
	   -OpenSolaris wasn't handling 3 pipes in a single line, reduced
	    to maximum of 2 and it works.
20101127 ver 0.8.3 A week trialing zxfer on the workstation in active use
             pays off. Not all snapshots were transferring with zfs send
	     based zxfers. Needless to say, very glad it's squashed.
	   -Also added a beep (-b) feature, to alert when zxfer finishes.
	   -Finished tweaking and testing the man page for easy conversion
	     to Solaris format (using mdoc2man.awk).
20101117 ver 0.8.2 Nearly ready, just needs Solaris man page.
           -Location of rsync is searched for at the start.
20101115 ver 0.8.1 Ready to release?
           -Finished testing on all of the examples in the man page.
           -Added the ability to specify snapshots to use as a base
	     for rsync (option "u").
	   -Changed the default rsync options (see man page).
20101013 ver 0.8.0 First "near final" draft sent to Constantin.
           In the intervening time, many changes have taken place.
	   zxfer is chosen as the new name. Some of the additional features:
	   -recursive transfers
	   -property and property source transfer of ZFS filesystems -
	     creation and setting
	   -property overrides (e.g. for compression, dedup, copies etc.)
	   -an rsync mode, for copying arbitrary files and directories
	     from within a pool atomically
	   -property backup mode, to allow the future restore of properties
	     while retaining the ability to back them up with different
	     properties than they would otherwise have, e.g. compression etc.
	   -removed the ksh dependency
	   -a man page
	   -much bug testing
20100504 Dreckman worked on a bug fix and some additions to
           zfs-replicate; these are submitted to CG. At this stage
	   the additions are already fairly extensive, so Contantin kindly
	   suggested that the new version become a new utility in its
	   own right. Collaboration commenced between the two with
	   Constantin offering helpful guidance throughout as Dreckman coded
	   new features and squashed bugs.
20080813 zfs-replicate 0.7 is released by Constantin Gonzalez as the
           last update in his zfs-replicate utility. This may be found at
         http://blogs.sun.com/constantin/entry/zfs_replicator_script_new_edition
- tests/run_integration_zxfer.sh now supports `--skip-test <function>` and
  `ZXFER_SKIP_TESTS` so specific integration cases can be skipped while the rest
  of the suite continues to run.
- tests/run_integration_zxfer.sh now supports `--keep-going` so integration runs
  can continue after failing cases and report the full failure list at the end.
